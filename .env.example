# Este arquivo serve como exemplo. Crie um arquivo .env na raiz do seu projeto
# com suas credenciais reais para uso local, e configure estas variáveis
# diretamente no painel do Vercel para o deploy de produção.

# Variáveis de Ambiente Necessárias para o Servidor (Backend)

# 1. Supabase (Banco de Dados)
# Você deve usar suas próprias credenciais de Supabase.
SUPABASE_URL=SUA_URL_DO_SUPABASE
SUPABASE_KEY=SUA_CHAVE_ANON_DO_SUPABASE

# 2. Mercado Pago (Pagamentos)
# Credenciais de Produção do Mercado Pago.
MP_PUBLIC_KEY=SUA_PUBLIC_KEY_DO_MP
MP_ACCESS_TOKEN=SEU_ACCESS_TOKEN_DO_MP
MP_CLIENT_ID=SEU_CLIENT_ID_DO_MP
MP_CLIENT_SECRET=SEU_CLIENT_SECRET_DO_MP

# 3. JWT (Autenticação)
# Chave secreta para assinar tokens JWT. Mantenha em segredo.
JWT_SECRET=SUA_CHAVE_SECRETA_JWT

# 4. Configuração do Servidor
# A porta é ignorada no Vercel, mas é útil para o desenvolvimento local.
PORT=3000
NODE_ENV=production

# 5. Variável de Ambiente para o Frontend (Vite/React)
# O Vercel injeta esta variável automaticamente.
# VITE_API_URL=https://[SEU-PROJETO].vercel.app/api
